// ---------find maximun from an array-------------- //
 function maxArray(arr)
 {
return Math.max(...arr)
 }
 const newArray = [1,2,3,4,5]
 console.log(maxArray(newArray))
 //--------find min from an Array -------//
  function minArray(arr)
  {
    return Math.min(...arr)
  }
  const myNewArray = [2,1,0,11,312]
  console.log(minArray(myNewArray))

  //-------find duplicate from an Array -----//

  const myNewArray1 = [1,2,3,1,2,4,5,4]

  //--------reverse an array-------------//
function reverseArray(arr)
{
    return arr.reverse();
}  
const number = [1,2,3,4,5]
console.log(reverseArray(number))

//--------remove Duplicate from an array-------------//